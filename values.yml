securityContexts: {}

workers:
  securityContexts: {}

redis:
  enabled: false
  
postgresql:
  enabled: false

data:
  brokerUrl: redis://:airflow@airflow-redis
  metadataConnection:
    host: airflow-psql
    user: postgres
    pass: airflow
